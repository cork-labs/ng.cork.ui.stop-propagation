describe('ng.cork.ui.stop-propagation', function () {
    'use strict';

    beforeEach(module('ng.cork.ui.stop-propagation'));

    describe('corkUiStopPropagation', function () {

        it('should pass a dummy test.', inject(function () {
            expect(true).toBe(true);
        }));

    });

});
